
- STOR should check DELE permission before overwriting existing files.

- Anonymous FTP using email address.

- MicroEMACS.

- Check for  command in progress before proceeding  with client request.
  Can't have  overlapping requests.  FTP to return 400 series "FTP busy"
  (try again) response to client.

- Add global try/except and bgerror to:

  - Avoid dying.
  - Avoid leaving the dimmer on.
  - Avoid leaving temporary files around.

